<section class="row {{ reverse ? 'row--reverse' : '' }}">

    {# {% if isSortable or isSearchable or isPaginated %} #}
        
    <turbo-frame id="{{ tableTitle|lower|replace({' ': '-'}) }}" style="display:contents">
        {% include 'components/_column-table.html.twig' with {


        } %}
    </turbo-frame>

    {# {% else %}
        
        {% include 'components/_column-table.html.twig' with {


        } %}
    {% endif %} #}




</section>