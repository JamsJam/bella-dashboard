{% block new_bodysize_form %}

	<turbo-stream action="replace" targets="#avatar_actions">
		<template>
			<div id="avatar_actions" class="avatar__navigation">
				<a class="button__action" data-turbo-frame="admin-main" href="{{path('app_avatar_bodysize_new_cancel')}}" data-turbo-action="replace">{{"annuler"|capitalize}}</a>
			</div>
		</template>
	</turbo-stream>

	<turbo-stream action="append" targets="#color__grid">
		<template>
			<div
				class="modal" id="color-form-modal">
				{# {{ form_start(form)}} #}
				{{ form_start(form, {'action': path('app_avatar_bodysize_new')}) }}
				{{form_row(form.name)}}


				<div class="button__container">
					<button class="button__action" type="submit">Envoyer</button>

				</div>
				{{ form_end(form)}}
			</div>
		</template>
	</turbo-stream>


{% endblock %}

{% block success_add_bodysize %}
	<turbo-stream action="remove" targets="#color-form-modal"></turbo-stream>

	<turbo-stream action="replace" targets="#avatar_actions">
		<template>
			<div id="avatar_actions" class="avatar__navigation">

					<a href="{{path('app_avatar_bodysize_new')}}" data-turbo-frame="_top" class="button__action">
						{{"Ajouter une taille"|capitalize}}
					</a>

			</div>
		</template>

	</turbo-stream>


	<turbo-stream action="append" targets="#color__grid">
		<template>
			<div class="grid__row" id="row-{{bodysize.id}}">

				<span></span>
				<span></span>

				<p>{{bodysize.name|capitalize}}</p>


				<div class="row__action">
					{# <a class="action__edit" href="{{path('app_avatar_bodysize_edit',{id:bodysize.id})}}" title="Modifier la taille">
						{{ ux_icon('mdi:edit-outline') }}
					</a> #}
					<form action="{{path('app_avatar_bodysize_delete',{id:bodysize.id})}}" method="post">
						<input type="hidden" name="_token" value="{{ csrf_token('delete-bodysize') }}">
						<button type="submit" class="action__delete" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cette taille ? Cette action entraînera la suppression de tout les morphotype et de toutes les pièces d\'avatar y faisant référence.');">
							{{ ux_icon('weui:delete-outlined') }}
						</button>
					</form>
				</div>
			</div>
		</template>
	</turbo-stream>


{% endblock %}

{% block delete_new_bodysize_form %}
	<turbo-stream action="replace" targets="#avatar_actions">
		<template>
			<div id="avatar_actions" class="avatar__navigation">
				<a 
					{# target="color__grid" #}
					href="{{path('app_avatar_bodysize_new')}}"
					data-turbo-frame="avatar-frame" 
					class="button__action"
					>
					{{"Ajouter une taille"|capitalize}}
				</a>
			</div>
		</template>
	</turbo-stream>
	
	<turbo-stream action="remove" targets="#color-form-modal"></turbo-stream>
{% endblock %}
