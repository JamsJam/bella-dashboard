<div class="data-table__actions">
    <a href="{{ showUrl|default('#') }}" class="data-table__action" aria-label="Voir la categorie">
        {{ ux_icon('ep:document') }}
        <span>Voir</span>
    </a>
    <form method="post" action="{{ deleteUrl }}" data-turbo-confirm="Supprimer cette categorie ?">
        <input type="hidden" name="_csrf_token" value="{{ deleteToken }}">
        <button
            type="submit"
            class="data-table__action data-table__action--danger"
            data-category-id="{{ categoryId }}"
            aria-label="Supprimer la categorie"
        >
            {{ ux_icon('ep:delete') }}
            <span>Supprimer</span>
        </button>
    </form>
</div>
