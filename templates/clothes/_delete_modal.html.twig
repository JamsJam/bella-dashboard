<div class="modal-backdrop">
    <dialog class="modal" open aria-labelledby="clothe-delete-modal-title">
        <form method="post" action="{{ action }}" data-turbo="false">
            <header class="modal__header">
                <h2 id="clothe-delete-modal-title">Archiver le vêtement</h2>
                <button type="button" class="modal__close" onclick="this.closest('.modal-backdrop').remove()" aria-label="Fermer">
                    &times;
                </button>
            </header>

            <div class="modal__body">
                <input type="hidden" name="_csrf_token" value="{{ csrfToken }}">
                <p>Archiver {{ clothe.name }} le retirera du catalogue sans supprimer ses données ni ses variantes.</p>
            </div>

            <footer class="modal__actions">
                <button type="button" class="clothe-show__button" onclick="this.closest('.modal-backdrop').remove()">
                    Annuler
                </button>
                <button type="submit" class="clothe-show__button clothe-show__button--danger">
                    Archiver
                </button>
            </footer>
        </form>
    </dialog>
</div>
